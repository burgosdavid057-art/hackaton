# Modelo (capa gratuita, sin tarjeta): texto, function calling, visión y embeddings
google-genai>=1.0.0
# Cliente del protocolo OpenAI: lo usa agent/openai_backend.py para hablarle a
# Ollama y a Groq. Sin esto, el camino local (el único que sirve en planta) ni
# siquiera importa.
openai>=1.40.0
# Carga de .env
python-dotenv>=1.0.0
# Extracción de manuales en PDF
pypdf>=5.0.0
# Base de datos vectorial para RAG
chromadb>=0.5.0
# Interfaz del demo
streamlit>=1.40.0
# Generación de la placa de datos de prueba (entrada multimodal)
pillow>=10.0.0
# Canal WhatsApp (webhook)
flask>=3.0.0

# --- Copiloto de Produccion (produccion/) ---
# Toda la aritmetica de herramientas.py. El LLM no calcula: esto si.
pandas>=2.0.0
# Motor de .xlsx para pandas.read_excel en ingest.a_texto
openpyxl>=3.1.0
# PDF con tablas: extract_text() solo pierde la correspondencia fila-columna
pdfplumber>=0.11.0
# taxonomia.yaml: 4 lineas, 26 estaciones, 45 causas con alias
PyYAML>=6.0
